#!/bin/sh
. $(dirname $0)/common.sh

rsync -avrP --delete --exclude=.git --exclude=Meta \
	"$TF_USER"@"$TF_SSH_HOST":"$TF_HTDOCS/" "$RELEASE_TREE/"

if test -d releases
then
	rsync -avrP --delete --exclude=HEADER.html --exclude=.gitignore \
		"$RELEASE_TREE/releases/" releases/
fi
